From 43b3a12b96bdde3332ca100f35ffcc8d75fb3c1e Mon Sep 17 00:00:00 2001
From: openclaw <openclaw@Macmini.fritz.box>
Date: Fri, 6 Mar 2026 18:24:24 +0100
Subject: [PATCH] fix(dsa5): address codex PR14 review findings

---
 .../mcp-server/src/systems/dsa5/character-creator.ts     | 2 +-
 .../mcp-server/src/systems/dsa5/json-actor-importer.ts   | 9 +++++----
 2 files changed, 6 insertions(+), 5 deletions(-)

diff --git a/packages/mcp-server/src/systems/dsa5/character-creator.ts b/packages/mcp-server/src/systems/dsa5/character-creator.ts
index 7e6d02b..232053c 100644
--- a/packages/mcp-server/src/systems/dsa5/character-creator.ts
+++ b/packages/mcp-server/src/systems/dsa5/character-creator.ts
@@ -284,7 +284,7 @@ export class DSA5CharacterCreator {
                   
                   if (speciesMatch && professionMatch) {
                     fullDataEntries.push({
-                      id: entry.id,
+                      id: entry._id || entry.id,
                       name: entry.name,
                       packId: pack.id,
                       packLabel: pack.label,
diff --git a/packages/mcp-server/src/systems/dsa5/json-actor-importer.ts b/packages/mcp-server/src/systems/dsa5/json-actor-importer.ts
index ae0f906..affd657 100644
--- a/packages/mcp-server/src/systems/dsa5/json-actor-importer.ts
+++ b/packages/mcp-server/src/systems/dsa5/json-actor-importer.ts
@@ -861,7 +861,11 @@ export class DSA5JsonActorImporter {
       const payload = await extractPayload(jsonPayload, filePath);
       const normalizedPayload = normalizeInputKeys(payload);
       const validation = validateImportPayload(normalizedPayload);
-      if (validation.errors.length > 0) {
+
+      const detectedFormat = validation.detectedFormat;
+      const selectedFormat = strategy === 'auto' ? detectedFormat : strategy;
+
+      if (strategy === 'auto' && validation.errors.length > 0) {
         return {
           success: false,
           error: validation.errors.join(' | '),
@@ -870,9 +874,6 @@ export class DSA5JsonActorImporter {
         };
       }
 
-      const detectedFormat = validation.detectedFormat;
-      const selectedFormat = strategy === 'auto' ? detectedFormat : strategy;
-
       let mappingResult: MappingResult;
       switch (selectedFormat) {
         case 'raw_foundry':
-- 
2.54.0

